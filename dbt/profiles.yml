retail_analytics:
  target: dev
  outputs:
    dev:
      type: postgres
      host: "{{ env_var('DBT_HOST', 'warehouse') }}"
      port: "{{ env_var('DBT_PORT', '5432') | int }}"
      user: postgres
      password: postgres
      dbname: retail_warehouse
      schema: analytics
      threads: 4